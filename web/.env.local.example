# Server Component fetch는 absolute URL 필요 — localhost direct
# 프로덕션 Cloudflare Tunnel 환경에선 NEXT_PUBLIC_API_BASE=https://mrms.approid.team/api 같이 설정
NEXT_PUBLIC_API_BASE=http://localhost:8000/api
